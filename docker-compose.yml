version: '3.8'

services:
  conexao-de-sorte-backend-scheduler:
    image: ghcr.io/wibson82/conexao-de-sorte-backend-scheduler:latest
    container_name: conexao-de-sorte-backend-scheduler
    
    # OTIMIZAÇÃO HOSTINGER KVM 2
    deploy:
      resources:
        limits:
          cpus: '0.4'
          memory: 400m
        reservations:
          cpus: '0.1'
          memory: 128m
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 3
        window: 60s
    
    # Configurações otimizadas
    restart: unless-stopped
    
    # Health check leve
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 90s
    
    # Variáveis de ambiente JVM otimizadas
    environment:
      - JAVA_OPTS=-Xms128m -Xmx350m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+UseStringDeduplication
      - SERVER_PORT=8081
      - SPRING_PROFILES_ACTIVE=prod
    
    # Secrets via Docker Swarm
    secrets:
      - conexao-de-sorte-database-password
      - conexao-de-sorte-redis-password
      - conexao-de-sorte-jwt-secret
    
    # Networks
    networks:
      - conexao-network-swarm
    
    # Logging otimizado
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

# Secrets externos
secrets:
  conexao-de-sorte-database-password:
    external: true
  conexao-de-sorte-redis-password:
    external: true
  conexao-de-sorte-jwt-secret:
    external: true

# Networks externos
networks:
  conexao-network-swarm:
    external: true
